This version is the SpringMVC startup code for
SWE students. It has the same business
and middleware tiers as MainProjectFX.

This version contains the version of DataAccess
that makes use of PreparedStatements

Spring MVC is used to provide the basic
idea for the first 4 screens of browse and select.
Student must figure out the rest.